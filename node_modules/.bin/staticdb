#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../staticdb/bin/staticdb" "$@"
  ret=$?
else 
  node  "$basedir/../staticdb/bin/staticdb" "$@"
  ret=$?
fi
exit $ret
